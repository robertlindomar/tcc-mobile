import { TelaEmBreve } from "@/components/TelaEmBreve";

export default function TelaMissoes() {
    return (
        <TelaEmBreve
            descricao="Em breve você verá as missões disponíveis e poderá acompanhar o que já concluiu."
            icone="flag-outline"
            titulo="Missões"
        />
    );
}
